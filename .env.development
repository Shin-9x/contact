# Used by `npm run dev`. Vite proxies /api to 127.0.0.1:8787, where `npm run dev:api`
# (Node mock) or `npm run dev:php` (real PHP endpoint) answers.
# Append ?simulate=error|rate-limit|slow to exercise the form's failure states.
VITE_CONTACT_ENDPOINT=/api/contatti.php
