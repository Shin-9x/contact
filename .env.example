# Endpoint that receives contact-form submissions as JSON (POST).
# Already set in .env.development and .env.production; override here only to point
# the form at a different backend. Leave empty to make the form invite visitors to
# call instead of sending.
VITE_CONTACT_ENDPOINT=/api/contatti.php
