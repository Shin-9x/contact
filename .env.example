# Endpoint that receives contact-form submissions as JSON (POST).
# Leave unset to keep the form inert: it will invite visitors to call instead.
# Example: VITE_CONTACT_ENDPOINT=/api/contatti.php
VITE_CONTACT_ENDPOINT=
